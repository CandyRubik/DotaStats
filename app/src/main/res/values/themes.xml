<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.DotaStats" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">?attr/primary_color</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/secondary</item>
        <item name="colorOnSecondary">@color/black</item>
        <item name="android:windowBackground">?attr/background_color</item>

        <item name="android:buttonStyle">@style/Widget.DotaStats.Button</item>
        <item name="background_color">@color/background_light</item>
        <item name="ripple_color">@color/ripple_color_light</item>
        <item name="primary_color">@color/primary_light</item>
        <item name="winLoseStatViewStyle">@style/WinLoseStatViewStyle</item>
    </style>

    <style name="Theme.DotaStats" parent="Base.Theme.DotaStats" />

</resources>